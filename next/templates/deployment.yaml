apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "next-app.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "next-app.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "next-app.name" . }}
    spec:
      containers:
        - name: next-app
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 3000
